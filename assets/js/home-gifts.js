import{db}from'./firebase-config.js';import{collection,getDocs,limit,query,where}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';const grid=document.querySelector('#homeGiftsGrid');const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');function available(g){const explicit=Number(g.availableQuantity);if(Number.isFinite(explicit))return Math.max(0,explicit);return Math.max(0,Number(g.quantity??0)-Number(g.reservedQuantity??0))}function render(items){if(!grid)return;if(!items.length){grid.innerHTML='<article class="content-card"><span class="tag">Em breve</span><h3>A lista está sendo preparada.</h3><p>Os itens aparecerão aqui assim que forem cadastrados.</p></article>';grid.setAttribute('aria-busy','false');return}grid.innerHTML=items.map(({id,data:g})=>{const qtd=available(g);return`<article class="content-card"><span class="tag">${esc(g.category||'Presente')}</span><h3>${esc(g.name||g.title||'Presente para a Iúna')}</h3><p>${esc(g.description||'Um gesto de carinho para a chegada da Iúna.')}</p><strong>${qtd>0?`${qtd} ${qtd===1?'unidade disponível':'unidades disponíveis'}`:'Item já escolhido'}</strong><a class="button ${qtd>0?'button-primary':'button-secondary'}" href="./presentes.html#${encodeURIComponent(id)}">${qtd>0?'Quero presentear':'Ver outros presentes'}</a></article>`}).join('');grid.setAttribute('aria-busy','false')}async function load(){if(!grid)return;try{const snap=await getDocs(query(collection(db,'gifts'),where('active','==',true),limit(6)));render(snap.docs.map(d=>({id:d.id,data:d.data()})))}catch(e){console.error('[HOME GIFTS]',e);grid.innerHTML='<article class="content-card"><span class="tag">Presentes</span><h3>Não foi possível carregar agora.</h3><p>Abra a lista completa para tentar novamente.</p><a class="button button-secondary" href="./presentes.html">Abrir lista</a></article>';grid.setAttribute('aria-busy','false')}}load();
